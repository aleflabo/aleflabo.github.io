# Tutti i testi del sito, alla lettera

> Estratto automaticamente dalle tavole in `tavole/`.
> **Questa è la fonte del copy: non riscrivere, copiare.**
> Se una frase non convince, si cambia qui *e* nella tavola, mai durante l'implementazione.

> I titoli della home sono spezzati in `<span>` una parola ciascuno perché entrano
> uno alla volta: qui sono ricomposti, nel codice vanno rispezzati.


---

## / — home

Sorgente: `tavole/Main.dc.html`

- `etichetta` Alessandro Flaborea
- `LINK` Servizi
- `LINK` Formazione
- `LINK` Lavori
- `LINK` Ricerca
- `LINK` Chi sono
- `LINK` Note
- `LINK` Parliamone
- `etichetta` IT / EN
- `testo` Consulenza e formazione sull'intelligenza artificiale

### Aiuto le aziende a capire dove serve l'intelligenza artificiale e poi la costruisco.

*TITOLO DI PAGINA (h1)*

- `testo` Tre anni di dottorato in computer vision, poi due da CTO di una startup che quella ricerca l'ha portata dentro aziende che producono. Adesso lavoro in proprio.
- `LINK` Prenota mezz'ora
- `etichetta` →
- `LINK` Guarda cosa ho costruito
- `testo` Dottorato in computer vision alla Sapienza · Pubblicato a CVPR, ICCV, IROS · Co-fondatore e CTO di
- `LINK` Procedo
- `testo` dal 2024 al 2026
- `etichetta` 10
- `etichetta` articoli pubblicati
- `etichetta` 533
- `etichetta` citazioni
- `etichetta` 1
- `etichetta` Best Paper Award
- `etichetta` 2
- `etichetta` anni da CTO
- `testo` Il problema

### Le tre cose che mi vengono raccontate più spesso

*TITOLO DI SEZIONE (h2)*

- `testo` «Mi serve questo pezzo.»
- `testo` Arriva una mail con una foto storta. Prima di poter fare un preventivo bisogna capire di quale componente si tratti, e succede che ci si mettano in due per un paio di giorni.
- `testo` «Lo sa fare solo Bruno.»
- `testo` Quarant'anni di mestiere che stanno nella testa di una persona sola. Nessuno ha mai avuto il tempo di metterli per iscritto, e fra due anni Bruno va in pensione.
- `testo` «Abbiamo provato ChatGPT.»
- `testo` Qualcuno in ufficio ha cominciato a usarlo per conto suo. Non è chiaro con quali documenti, né se l'azienda sia d'accordo, e finora nessuno ha avuto occasione di parlarne.
- `testo` Come lavoro

### Come si svolge di solito un lavoro

*TITOLO DI SEZIONE (h2)*

- `testo` Quattro passi. Ci si può fermare dopo ognuno, e quello che è stato fatto fino a lì resta comunque tuo.
- `etichetta` 01

### Ascolto

*SOTTOTITOLO (h3)*

- `testo` Passo mezza giornata in azienda, insieme alle persone che fanno il lavoro tutti i giorni. Serve a me per capire come funzionate, e a te per farti un'idea di come ragiono.
- `testo` Ne esce
- `testo` Una lista dei punti dove si perde tempo, ordinata per quanto costano.
- `etichetta` 02

### Diagnosi

*SOTTOTITOLO (h3)*

- `testo` Due o tre settimane per guardare i dati che avete davvero, i gestionali già in uso e chi si occupa di cosa. Alla fine scrivo cosa converrebbe fare, in che ordine, e quali strade lascerei perdere.
- `testo` Ne esce
- `testo` Un documento che resta tuo, e che puoi far leggere a chi vuoi.
- `etichetta` 03

### Costruisco

*SOTTOTITOLO (h3)*

- `testo` Comincio sempre dalla parte che potrebbe non funzionare, così se qualcosa si rivela impossibile lo scopriamo presto, quando cambiare strada costa ancora poco.
- `testo` Ne esce
- `testo` Qualcosa che i tuoi aprono il lunedì mattina e usano davvero.
- `etichetta` 04

### Resto

*SOTTOTITOLO (h3)*

- `testo` Un software che nessuno cura smette di essere utile nel giro di qualche mese. Se serve restiamo in contatto: manutenzione, server e le modifiche che vengono fuori strada facendo.
- `testo` Ne esce
- `testo` Una persona che conosce il sistema e che puoi chiamare quando serve.
- `LINK` I quattro modi di lavorare insieme, per esteso →
- `testo` Le prove

### Alcune cose che ho costruito

*TITOLO DI SEZIONE (h2)*

- `testo` Meccanica · commessa reale

### Portale ricambi 3D

*SOTTOTITOLO (h3)*

- `testo` Il cliente apre il modello 3D della macchina che ha comprato, clicca il pezzo che gli serve, e in azienda la richiesta arriva già associata al codice interno di quel componente.
- `etichetta` Pipeline CAD, 80 test
- `etichetta` Proprietà intellettuale protetta
- `LINK` Come funziona →
- `testo` Industria · in produzione

### Procedo

*SOTTOTITOLO (h3)*

- `testo` I video girati in reparto diventano procedure passo-passo, e chi lavora può fare domande alla documentazione della propria azienda. Ne sono stato co-fondatore e CTO per due anni; oggi la seguo come advisor.
- `etichetta` Clienti industriali
- `etichetta` B4i Bocconi
- `etichetta` Round angel
- `LINK` Come funziona →
- `testo` Prodotto vivo · aperto a tutti

### Spannum

*SOTTOTITOLO (h3)*

- `testo` Un test della memoria di lavoro costruito seguendo il protocollo pubblicato in letteratura, con le fonti citate e i limiti dichiarati. È online e si può provare.
- `etichetta` Online
- `etichetta` Norme per età
- `LINK` Aprilo →
- `LINK` Tutti i lavori, compresa la ricerca →
- `testo` Formazione

### Si comincia dalle persone

*TITOLO DI SEZIONE (h2)*

- `testo` Quattro ore, in azienda o in aula, per mostrare alle persone cosa questi strumenti sanno fare e quali sono i loro limiti. Si lavora su casi concreti.
- `testo` Dal febbraio 2025 l'AI Act chiede alle aziende che usano strumenti di intelligenza artificiale di garantire una formazione minima al personale, e di poterlo documentare. Quel corso lo farete comunque: tanto vale che serva a qualcosa.
- `testo` 4 ore

### Corso introduttivo

*SOTTOTITOLO (h3)*

- `testo` Per tutta l'azienda, dal titolare al reparto. Cosa è cambiato negli ultimi anni, cosa è rimasto uguale, e quali sono i rischi concreti per i vostri dati.
- `testo` 8-12 ore

### Laboratorio per funzione

*SOTTOTITOLO (h3)*

- `testo` Ufficio tecnico, acquisti, amministrazione, commerciale. Ogni gruppo lavora sui propri documenti, così quello che si impara si può usare il giorno dopo.
- `testo` Durata concordata

### Percorso finanziato

*SOTTOTITOLO (h3)*

- `testo` Attraverso enti accreditati e fondi interprofessionali, quando l'azienda vuole un percorso lungo senza impegnare cassa propria.
- `testo` Ho insegnato quattro anni alla Sapienza come assistente alla didattica, e sono stato relatore invitato al Data Science Hub di Ferrari S.p.A. I corsi si organizzano direttamente oppure attraverso enti accreditati e fondi interprofessionali.
- `LINK` Come si organizza un corso →
- `testo` Da dove viene quello che so

### Dalla ricerca alla produzione

*TITOLO DI SEZIONE (h2)*

- `testo` Una magistrale in data science, poi un dottorato in computer vision alla Sapienza, con il lavoro pubblicato in conferenze dove viene esaminato a fondo prima di uscire. Sono poi stato due anni CTO di una startup industriale, che è il mestiere in cui si scopre quanto di quella ricerca sopravvive al contatto con un'azienda vera.
- `testo` Ogni articolo che ho pubblicato esce insieme al codice. È l'abitudine con cui lavoro anche adesso: le decisioni tecniche restano scritte e il software che consegno si apre e si legge.
- `testo` 2021–2024
- `testo` Dottorato in computer vision, Sapienza — PINlab
- `testo` CVPR · ICCV · IROS
- `testo` Le conferenze principali del settore
- `testo` 2024–2026
- `testo` Co-fondatore e CTO di Procedo, startup industriale
- `LINK` La ricerca, per intero →
- `LINK` Chi sono →
- `testo` Parliamone

### Mezz'ora per capire se possiamo lavorare insieme

*TITOLO DI SEZIONE (h2)*

- `testo` Se non è il lavoro giusto per me te lo dico subito, e se conosco qualcuno più adatto ti metto in contatto.
- `campo modulo` Azienda
- `testo` Ragione sociale
- `campo modulo` Come ti scrivo
- `testo` Mail o telefono
- `campo modulo` Cosa vorresti smettere di fare a mano
- `testo` Bastano due righe, anche buttate lì
- `campo modulo` Quanto è urgente
- `etichetta` Sto solo guardando
- `etichetta` Quest'anno
- `etichetta` Ho un problema adesso
- `LINK` Manda
- `testo` Oppure, se preferisci saltare il modulo:
- `LINK` Scegli tu mezz'ora sul calendario
- `LINK` WhatsApp
- `LINK` Mail
- `testo` Alessandro Flaborea
- `testo` Consulenza e formazione sull'intelligenza artificiale. Dalla ricerca al software che le persone usano.
- `LINK` Prenota mezz'ora
- `testo` Cosa faccio
- `LINK` Servizi
- `LINK` Formazione
- `LINK` Che fine fanno i vostri dati
- `testo` Le prove
- `LINK` Lavori
- `LINK` Ricerca
- `LINK` Chi sono
- `LINK` Note
- `testo` Altrove
- `LINK` LinkedIn
- `LINK` GitHub
- `LINK` Google Scholar
- `LINK` English site
- `etichetta` © 2026 Alessandro Flaborea · P. IVA [DA INSERIRE]
- `LINK` Privacy
- `LINK` Cookie

---

## /servizi

Sorgente: `tavole/Servizi.dc.html`

- `etichetta` Alessandro Flaborea
- `LINK` Servizi
- `LINK` Formazione
- `LINK` Lavori
- `LINK` Ricerca
- `LINK` Chi sono
- `LINK` Note
- `LINK` Parliamone
- `testo` Servizi

### Quattro modi di lavorare insieme

*TITOLO DI PAGINA (h1)*

- `testo` Si parte dal primo. Ogni passo è a sé: si prosegue quando il lavoro lo richiede, ci si può fermare in qualsiasi momento, e quello che è stato fatto resta comunque vostro.
- `testo` Capire

### Mezza giornata diagnostica

*TITOLO DI SEZIONE (h2)*

- `testo` Vengo in azienda, guardo come lavorate e faccio un po' di domande. Serve a capire se c'è qualcosa che vale la pena affrontare adesso, e a volte la conclusione è che si può rimandare.
- `testo` Per chi
- `testo` Non è chiaro da dove cominciare
- `testo` Quanto dura
- `testo` Una mattina
- `testo` Cosa resta
- `testo` Una lista dei punti dove si perde tempo, con accanto quanto costano
- `testo` Decidere

### Audit AI

*TITOLO DI SEZIONE (h2)*

- `testo` Guardo i dati che avete davvero, i gestionali già in uso, chi si occupa di cosa e con quali vincoli. Poi scrivo cosa converrebbe fare, in che ordine, e quali strade lascerei perdere.
- `testo` Il documento resta vostro. Se un domani il lavoro passa a qualcun altro, glielo consegnate e riparte da lì.
- `testo` Per chi
- `testo` Hai un'idea e vuoi sapere se regge prima di metterci soldi
- `testo` Quanto dura
- `testo` Due o tre settimane
- `testo` Cosa resta
- `testo` Un documento che potete far leggere a chiunque, compresi altri fornitori
- `testo` Costruire

### Software su misura

*TITOLO DI SEZIONE (h2)*

- `testo` Dall'automazione di un singolo passaggio al sistema completo. Comincio sempre dalla parte che potrebbe non funzionare, così se qualcosa si rivela impossibile lo scopriamo quando cambiare strada costa ancora poco.
- `testo` Il codice è vostro e ve lo consegno, così non restate legati a me più del necessario.
- `testo` Per chi
- `testo` Hai un processo preciso che vuoi togliere di mezzo
- `testo` Quanto dura
- `testo` Da sei settimane, a scaglioni che approvate uno alla volta
- `testo` Cosa resta
- `testo` Qualcosa che le persone usano davvero, più il codice sorgente
- `testo` Mantenere

### Canone di servizio

*TITOLO DI SEZIONE (h2)*

- `testo` Un software che nessuno cura smette di essere utile nel giro di qualche mese. Il canone tiene in piedi quello che abbiamo costruito: server, aggiornamenti e le ore di modifica che servono strada facendo.
- `testo` Dodici mesi rinnovabili, con uscita su preavviso. Le ore incluse sono scritte nel contratto.
- `testo` Per chi
- `testo` Hai qualcosa in produzione e preferiresti non doverci pensare
- `testo` Quanto dura
- `testo` Dodici mesi, rinnovabili, con uscita
- `testo` Cosa resta
- `testo` Server compresi, e una persona che risponde quando chiami
- `LINK` Le prove: quello che ho costruito →
- `testo` Onestà

### Per chi non sono

*TITOLO DI SEZIONE (h2)*

- `testo` Se cerchi un sito vetrina o un e-commerce, ci sono agenzie che lo fanno meglio di me e costano meno.
- `testo` Se l'obiettivo è introdurre l'intelligenza artificiale senza avere in mente un problema preciso, conviene fermarsi prima e cercare il problema.
- `testo` Se serve qualcuno presente tutti i giorni, conviene assumere una persona invece di chiamare un consulente.
- `testo` Se i dati che servirebbero non esistono o nessuno li ha mai messi per iscritto, il primo lavoro è raccoglierli, e richiede tempo.
- `testo` Domande

### Quelle che mi vengono fatte quasi sempre

*TITOLO DI SEZIONE (h2)*


### Dove finiscono i nostri dati?

*SOTTOTITOLO (h3)*

- `testo` È la domanda giusta e merita più di un paragrafo: c'è una pagina intera che la spiega, comprese le cose che non posso garantirvi. In breve: i file restano vostri e nessuno li usa per addestrare niente; quello che costruisco vive su un'infrastruttura intestata a voi.
- `LINK` Leggi la pagina sui dati →

### Quanto costa?

*SOTTOTITOLO (h3)*

- `testo` Dipende da tre cose: quanto è complicata la parte più difficile, quanti sistemi che avete già deve toccare, e se serve che qualcuno lo tenga in vita dopo. Non pubblico un listino perché un prezzo scritto senza conoscere il vostro caso sarebbe un numero inventato. Alla fine della mezza giornata diagnostica avete un preventivo scritto con le voci separate, e nessun obbligo di proseguire.

### Quanto ci vuole?

*SOTTOTITOLO (h3)*

- `testo` La mezza giornata è una mattina. L'audit due o tre settimane. Un progetto parte da sei settimane e procede a scaglioni che approvate uno alla volta, così potete fermarvi a un punto qualsiasi senza perdere quello che è stato fatto.

### Cosa serve da voi?

*SOTTOTITOLO (h3)*

- `testo` Poco, ma quel poco è indispensabile: mezza giornata delle persone che fanno il lavoro davvero, non solo di chi lo racconta; qualche file vero invece che un esempio ripulito; e una persona interna che possa rispondere alle domande quando ne nascono. Senza quest'ultima i progetti si fermano, sempre.

### E se poi non funziona?

*SOTTOTITOLO (h3)*

- `testo` Comincio dalla parte che potrebbe non funzionare proprio per scoprirlo presto. Se salta fuori che una cosa non si può fare, ve lo dico, vi consegno quello che ho capito fino a lì e ci fermiamo: pagate il lavoro fatto e non il risultato promesso. È successo, e sono stati soldi risparmiati.

### Come si comincia

*TITOLO DI SEZIONE (h2)*

- `testo` Mi scrivi due righe su cosa vorreste smettere di fare a mano e ci sentiamo mezz'ora. Se c'è margine per lavorare insieme ci vediamo di persona; altrimenti te lo dico subito, e se conosco qualcuno più adatto ti metto in contatto.
- `LINK` Parliamone, mezz'ora
- `testo` Alessandro Flaborea
- `testo` Consulenza e formazione sull'intelligenza artificiale. Dalla ricerca al software che le persone usano.
- `LINK` Prenota mezz'ora
- `testo` Cosa faccio
- `LINK` Servizi
- `LINK` Formazione
- `LINK` Che fine fanno i vostri dati
- `testo` Le prove
- `LINK` Lavori
- `LINK` Ricerca
- `LINK` Chi sono
- `LINK` Note
- `testo` Altrove
- `LINK` LinkedIn
- `LINK` GitHub
- `LINK` Google Scholar
- `LINK` English site
- `etichetta` © 2026 Alessandro Flaborea · P. IVA [DA INSERIRE]
- `LINK` Privacy
- `LINK` Cookie

---

## /formazione

Sorgente: `tavole/Formazione.dc.html`

- `etichetta` Alessandro Flaborea
- `LINK` Servizi
- `LINK` Formazione
- `LINK` Lavori
- `LINK` Ricerca
- `LINK` Chi sono
- `LINK` Note
- `LINK` Parliamone
- `testo` Formazione

### Corsi che si usano il giorno dopo

*TITOLO DI PAGINA (h1)*

- `testo` In aula si lavora sui documenti dell'azienda e sui casi che capitano davvero. L'obiettivo è che ognuno esca con qualcosa che può mettere in pratica subito.
- `testo` Una cosa che conviene sapere
- `testo` Dal 2 febbraio 2025 l'articolo 4 dell'AI Act chiede alle aziende che usano strumenti di intelligenza artificiale di garantire una formazione minima al personale, e di essere in grado di documentarla. Vale anche per chi usa soltanto ChatGPT, e non riguarda solo l'ufficio informatico.
- `testo` Il corso di base che soddisfa quella richiesta è dell'ordine delle quattro ore. È una formazione che l'azienda farà comunque: vale la pena che serva anche a lavorare meglio.
- `LINK` Che fine fanno i vostri dati →
- `testo` 4 ore

### Corso introduttivo

*TITOLO DI SEZIONE (h2)*

- `testo` Per tutta l'azienda insieme, dal titolare al reparto. Come funzionano questi strumenti, in che modo sbagliano e come ci si accorge che stanno sbagliando, che fine fanno i documenti che ci si incolla dentro, e quali sono i casi in cui conviene davvero usarli.
- `testo` Per chi
- `testo` Tutti, compreso chi non ha mai aperto ChatGPT
- `testo` Serve da voi
- `testo` Una sala e un proiettore
- `testo` 8-12 ore

### Laboratorio per funzione

*TITOLO DI SEZIONE (h2)*

- `testo` Ufficio tecnico, acquisti, amministrazione, commerciale: ogni gruppo lavora sui propri documenti e sui propri casi. Si parte da un'attività che oggi occupa ore e si prova a ridurla, insieme, durante la lezione.
- `testo` Per chi
- `testo` Un reparto alla volta, gruppi piccoli
- `testo` Serve da voi
- `testo` Documenti reali e il permesso di usarli
- `testo` Durata concordata

### Percorso finanziato

*TITOLO DI SEZIONE (h2)*

- `testo` Quando serve un percorso lungo senza impegnare cassa. Si costruisce insieme a un ente accreditato, che si occupa della parte amministrativa e della rendicontazione.
- `testo` Per chi
- `testo` Aziende con dipendenti già iscritte a un fondo
- `testo` Serve da voi
- `testo` Sapere a quale fondo siete iscritti. Al resto si pensa insieme
- `testo` Come si organizza

### Chi sostiene il costo del corso

*TITOLO DI SEZIONE (h2)*


### Direttamente

*SOTTOTITOLO (h3)*

- `testo` Mi contattate, concordiamo data e contenuti, fatturo all'azienda. È la strada più rapida: si può partire in un paio di settimane.

### Tramite ente accreditato

*SOTTOTITOLO (h3)*

- `testo` L'ente organizza e io insegno. È la strada da percorrere quando il corso deve rientrare in un catalogo regionale o avere un riconoscimento formale.

### Con i fondi interprofessionali

*SOTTOTITOLO (h3)*

- `testo` Le aziende con dipendenti versano già lo 0,30% del monte salari a un fondo come Fondimpresa. Quel denaro può essere usato per formare le persone, e serve un ente che presenti il piano.
- `testo` Chi sale in cattedra

### Vengo dall'aula

*TITOLO DI SEZIONE (h2)*

- `testo` Quattro anni come assistente alla didattica alla Sapienza, dal 2019 al 2023, durante il dottorato. Poi due anni a costruire software che le persone usano ogni giorno. In aula servono entrambe: la prima per riuscire a farsi capire, la seconda per insegnare cose che in azienda reggono.
- `testo` Sono stato anche relatore invitato al Data Science Hub di Ferrari S.p.A.
- `testo` Onestà

### Che cosa questo corso non è

*TITOLO DI SEZIONE (h2)*

- `testo` Non è un corso di programmazione. Si lavora sugli strumenti che esistono già e su come portarli nel lavoro di tutti i giorni. Scrivere codice e addestrare modelli è un percorso a parte.
- `testo` Non ci sono formule pronte da copiare. Le liste di trucchi smettono di funzionare al primo cambio di modello, mentre aver capito come ragiona lo strumento resta.
- `testo` Non arrivo con una percentuale di risparmio già scritta sulle slide. Prima di aver visto come lavorate sarebbe un numero inventato.

### Raccontami che azienda siete

*TITOLO DI SEZIONE (h2)*

- `testo` Quante persone siete, cosa fate e chi userebbe questi strumenti. Da lì si capisce quale delle tre strade conviene.
- `LINK` Prenota mezz'ora
- `LINK` WhatsApp
- `testo` Alessandro Flaborea
- `testo` Consulenza e formazione sull'intelligenza artificiale. Dalla ricerca al software che le persone usano.
- `LINK` Prenota mezz'ora
- `testo` Cosa faccio
- `LINK` Servizi
- `LINK` Formazione
- `LINK` Che fine fanno i vostri dati
- `testo` Le prove
- `LINK` Lavori
- `LINK` Ricerca
- `LINK` Chi sono
- `LINK` Note
- `testo` Altrove
- `LINK` LinkedIn
- `LINK` GitHub
- `LINK` Google Scholar
- `LINK` English site
- `etichetta` © 2026 Alessandro Flaborea · P. IVA [DA INSERIRE]
- `LINK` Privacy
- `LINK` Cookie

---

## /lavori

Sorgente: `tavole/Lavori.dc.html`

- `etichetta` Alessandro Flaborea
- `LINK` Servizi
- `LINK` Formazione
- `LINK` Lavori
- `LINK` Ricerca
- `LINK` Chi sono
- `LINK` Note
- `LINK` Parliamone
- `testo` Lavori

### Quello che ho costruito

*TITOLO DI PAGINA (h1)*

- `testo` Sopra il software pensato per il lavoro di un'azienda; sotto i prodotti, i giochi e le cose che ho costruito per conto mio. Ogni scheda dice com'è nato e a che punto è.

### Software per le aziende

*TITOLO DI SEZIONE (h2)*

- `etichetta` 2024–2026
- `testo` Industria · in produzione

### Procedo

*SOTTOTITOLO (h3)*

- `testo` Una piattaforma che trasforma i video girati in reparto in procedure passo-passo, e permette a chi lavora di fare domande alla documentazione della propria azienda. Ne sono stato co-fondatore e CTO per due anni: ho guidato l'architettura e portato in produzione la parte di AI. Oggi la seguo come advisor.
- `etichetta` Computer vision
- `etichetta` LLM applicati
- `etichetta` Dati isolati per cliente
- `etichetta` B4i Bocconi
- `etichetta` Round angel
- `etichetta` 2026
- `testo` Meccanica · commessa reale

### Portale ricambi 3D

*SOTTOTITOLO (h3)*

- `testo` Il cliente apre il modello 3D della macchina che ha comprato, clicca il pezzo che gli serve, e in azienda la richiesta arriva già associata al codice interno del componente. Oggi quella stessa richiesta è una mail con una foto, e prima del preventivo qualcuno deve andare a caccia del codice. La pipeline di conversione dei CAD è finita e collaudata; il portale è in costruzione.
- `etichetta` Pipeline CAD
- `etichetta` 80 test
- `etichetta` IP protetta
- `etichetta` In costruzione
- `etichetta` 2026
- `testo` Automotive · gestionale

### Tire Hub

*SOTTOTITOLO (h3)*

- `testo` Una dashboard gestionale per il settore gomme: anagrafiche, statistiche per area e il lavoro quotidiano di chi sta al banco. Nasce da un'iniziativa mia e non da una commessa: l'ho costruita partendo da uno strumento di generazione automatica, per poi rifare a mano le parti che contano e vedere quanto lontano si arriva.
- `etichetta` Gestionale
- `etichetta` Statistiche per area
- `etichetta` Iniziativa mia
- `etichetta` 2025
- `testo` Monitoraggio · uso interno

### Cost Tracking

*SOTTOTITOLO (h3)*

- `testo` Una dashboard che tiene insieme la spesa giornaliera su cloud, GitHub e servizi LLM di un piccolo team, con cambio valuta e ammortamento degli abbonamenti. Gira da sola con lavori schedulati e una tabella che registra ogni sincronizzazione, così quando qualcosa non torna si vede dove.
- `etichetta` Più fornitori in una vista
- `etichetta` Sincronizzazione giornaliera
- `etichetta` Gira senza supervisione

### Prodotti e strumenti miei

*TITOLO DI SEZIONE (h2)*

- `testo` Nati da una curiosità o da un bisogno mio. Alcuni hanno utenti veri e si possono aprire adesso; uno è un video.
- `etichetta` 2026
- `testo` Psicometria applicata · online

### Spannum

*SOTTOTITOLO (h3)*

- `testo` Un test della memoria di lavoro costruito seguendo il protocollo pubblicato in letteratura, con le norme per età e le fonti citate. Dichiara anche cosa le evidenze non sostengono: l'allenamento migliora il compito, non l'intelligenza. È aperto a tutti e si può provare adesso.
- `etichetta` Online e pubblico
- `etichetta` Norme per età
- `etichetta` Si installa sul telefono
- `etichetta` 2026
- `testo` Gamification · con utenti veri

### Fanta Mundial

*SOTTOTITOLO (h3)*

- `testo` Una lega di pronostici sul Mondiale con una quarantina di giocatori attivi: account, pronostici con finestre di chiusura, punteggi, classifica dal vivo e un lavoro schedulato che recupera gli esiti da una API esterna e aggiorna tutto senza che nessuno tocchi niente.
- `etichetta` Una quarantina di giocatori
- `etichetta` Esiti aggiornati da soli
- `etichetta` In produzione
- `etichetta` 2026
- `testo` Tooling agentico · uso quotidiano

### Agentic Workflow Toolkit

*SOTTOTITOLO (h3)*

- `testo` Una serie di agenti che automatizzano le attività ripetitive di un team tecnico: lo standup che incrocia l'attività su GitHub con i task su Notion, la documentazione di architettura che si rigenera leggendo il codice, e la revisione dei prompt prima che vadano in produzione.
- `etichetta` Standup automatico
- `etichetta` Documentazione rigenerata
- `etichetta` Revisione dei prompt
- `etichetta` 2026
- `testo` Gioco sociale · online

### Paesello

*SOTTOTITOLO (h3)*

- `testo` Il gioco dell'intruso, con la differenza che le parole le scrivono i giocatori e vengono dai posti del loro paese: il bar di Nino, il muretto davanti alla scuola. Il telefono mostra a tutti la stessa parola tranne a uno, il forestiero, che deve parlarne senza sapere di cosa si tratta. Sotto c'è un impianto in cui la sicurezza sta nel database e ogni riga appartiene a chi l'ha scritta, con i test automatici che girano prima di ogni pubblicazione.
- `etichetta` Gioco da telefono
- `etichetta` Parole scritte dai giocatori
- `etichetta` In produzione
- `etichetta` 2025
- `testo` Video generativo

### Una storia in più scene

*SOTTOTITOLO (h3)*

- `testo` Un video per un matrimonio, generato per intero, con una storia che attraversa diverse scene. Generare una scena è facile e lo fanno tutti; la parte difficile è generarne dieci in cui le stesse persone restano riconoscibili — stesso viso, stessi vestiti, stessa luce — e il racconto tiene dal principio alla fine. Da lì è nato anche il lavoro sulle immagini: mi interessa il punto in cui questi strumenti smettono di essere una dimostrazione e cominciano a reggere una consegna.
- `etichetta` Video generativo
- `etichetta` Continuità fra le scene
- `etichetta` Immagini
- `etichetta` SCENA 1
- `etichetta` SCENA 2
- `etichetta` SCENA 3
- `etichetta` SCENA 4
- `testo` [ Quattro fotogrammi dal video, scelti per far vedere che i protagonisti restano gli stessi. Meglio ancora: il video stesso ]
- `etichetta` 2025
- `testo` Dati civici · geospaziale

### Contrada Navigator

*SOTTOTITOLO (h3)*

- `testo` Le regole storiche che assegnano ogni via di un comune alla sua contrada, compresi i numeri civici pari e dispari, ricostruite dai registri e messe su una mappa navigabile. Con i trend di partecipazione dal 2012 e una proiezione fino al 2034.
- `etichetta` Mappa interattiva
- `etichetta` Serie storica 2012-2025
- `etichetta` Proiezioni al 2034
- `etichetta` 2024–oggi
- `testo` Sistemi di conoscenza

### Personal Vault

*SOTTOTITOLO (h3)*

- `testo` Una base di conoscenza personale in cui una casella d'ingresso in sola aggiunta alimenta una wiki curata da un agente, con schemi per clienti, progetti, persone e concetti, collegamenti fra le voci e comandi per ingerire, interrogare e verificare. La uso tutti i giorni.
- `etichetta` Wiki curata da un agente
- `etichetta` Uso quotidiano
- `etichetta` Vincoli sui dati
- `etichetta` 2021–2023
- `testo` Computer vision · MVP

### HomeSweatHome

*SOTTOTITOLO (h3)*

- `testo` Un personal trainer che dalla webcam di un portatile legge la postura e corregge l'esecuzione mentre ti alleni, senza mandare niente nel cloud perché il ritardo renderebbe il consiglio inutile. Ho progettato l'architettura e costruito la parte di percezione. Arrivato a MVP funzionante, presentato all'IdeaBoosterLab della Bocconi.
- `etichetta` Lettura della postura
- `etichetta` Tempo reale
- `etichetta` Senza cloud

### E poi c'è la ricerca

*TITOLO DI SEZIONE (h2)*

- `testo` Dieci articoli pubblicati, oltre cinquecento citazioni e il codice di tutti quelli che si potevano aprire. È da lì che viene la parte di questi progetti che non si improvvisa.
- `LINK` Vai alla ricerca →
- `LINK` Come lavoro →
- `testo` Alessandro Flaborea
- `testo` Consulenza e formazione sull'intelligenza artificiale. Dalla ricerca al software che le persone usano.
- `LINK` Prenota mezz'ora
- `testo` Cosa faccio
- `LINK` Servizi
- `LINK` Formazione
- `LINK` Che fine fanno i vostri dati
- `testo` Le prove
- `LINK` Lavori
- `LINK` Ricerca
- `LINK` Chi sono
- `LINK` Note
- `testo` Altrove
- `LINK` LinkedIn
- `LINK` GitHub
- `LINK` Google Scholar
- `LINK` English site
- `etichetta` © 2026 Alessandro Flaborea · P. IVA [DA INSERIRE]
- `LINK` Privacy
- `LINK` Cookie

---

## /ricerca

Sorgente: `tavole/Ricerca.dc.html`

- `etichetta` Alessandro Flaborea
- `LINK` Servizi
- `LINK` Formazione
- `LINK` Lavori
- `LINK` Ricerca
- `LINK` Chi sono
- `LINK` Note
- `LINK` Parliamone
- `testo` Ricerca

### La ricerca da cui viene il resto

*TITOLO DI PAGINA (h1)*

- `testo` Dottorato al Perception and Intelligence Lab della Sapienza, dal 2021 al 2024, e un periodo di ricerca all'Università di Amsterdam. Anomaly detection, riconoscimento delle azioni e apprendimento delle procedure: capire cosa sta facendo una persona guardandola.
- `testo` 10
- `testo` articoli pubblicati
- `testo` 533
- `testo` citazioni, ad agosto 2026
- `testo` 3
- `testo` da primo autore
- `testo` 1
- `testo` Best Paper Award
- `testo` Cosa c'entra con la tua azienda
- `testo` Senza gergo: ho passato anni a insegnare a un computer a guardare una persona che lavora e ad accorgersi che sta sbagliando un passaggio, mentre lo sta facendo.
- `testo` È la stessa domanda che si fa un capo reparto quando gira fra le postazioni. Da lì è nato Procedo, e da lì viene il modo in cui affronto i problemi che mi portano: prima si guarda come il lavoro viene fatto davvero, poi si decide cosa automatizzare.
- `LINK` Dove quella ricerca è diventata un prodotto →

### Le pubblicazioni

*TITOLO DI SEZIONE (h2)*

- `testo` Dalla più recente. Dove esiste il repository pubblico sono indicate le stelle su GitHub.
- `etichetta` 2026

### TI-PREGO: Chain of Thought and In-Context Learning for Online Mistake Detection in Procedural Egocentric Videos

*SOTTOTITOLO (h3)*

- `testo` Accorgersi in tempo reale che qualcuno sta sbagliando un passaggio, guardando il video dal suo punto di vista.
- `etichetta` Computer Vision and Image Understanding
- `etichetta` 11 citazioni
- `etichetta` 2025

### Compositional Entailment Learning for Hyperbolic Vision-Language Models

*SOTTOTITOLO (h3)*

- `testo` Modelli che legano immagini e parole in uno spazio geometrico che rispetta le gerarchie.
- `etichetta` ICLR
- `etichetta` 118 citazioni
- `etichetta` 2024

### Contracting Skeletal Kinematics for Human-Related Video Anomaly Detection

*SOTTOTITOLO (h3)*

- `testo` Riconoscere comportamenti anomali dal solo movimento dello scheletro. Primo autore.
- `etichetta` Pattern Recognition
- `etichetta` 65 citazioni · 8 stelle
- `etichetta` 2024

### PREGO: Online Mistake Detection in Procedural Egocentric Videos

*SOTTOTITOLO (h3)*

- `testo` Rilevare gli errori in una procedura mentre viene eseguita. Primo autore.
- `etichetta` CVPR
- `etichetta` 50 citazioni · 34 stelle
- `etichetta` 2024

### Hyp2Nav: Hyperbolic Planning and Curiosity for Crowd Navigation

*SOTTOTITOLO (h3)*

- `testo` Far muovere un robot in mezzo alla gente senza urtare nessuno.
- `etichetta` IROS
- `etichetta` 3 citazioni
- `etichetta` 2023

### Multimodal Motion Conditioned Diffusion Model for Skeleton-Based Video Anomaly Detection

*SOTTOTITOLO (h3)*

- `testo` Il più citato e il più usato dei miei lavori. Primo autore.
- `etichetta` ICCV
- `etichetta` 147 citazioni · 93 stelle
- `etichetta` 2023

### Are We Certain It's Anomalous?

*SOTTOTITOLO (h3)*

- `testo` Quando il modello dice «è un'anomalia», quanto ci si può credere. Presentazione spotlight.
- `etichetta` CVPR Workshops (VAND)
- `etichetta` 15 citazioni · 26 stelle
- `etichetta` 2023

### Best Practices for 2-Body Pose Forecasting

*SOTTOTITOLO (h3)*

- `testo` Prevedere come si muoveranno due persone che interagiscono. Best Paper Award.
- `etichetta` CVPR Workshops (Precognition)
- `etichetta` 17 citazioni
- `etichetta` 2023

### A Self-Supervised Algorithm to Detect Signs of Social Isolation in the Elderly

*SOTTOTITOLO (h3)*

- `testo` Riconoscere segnali di isolamento sociale negli anziani senza dati etichettati.
- `etichetta` Artificial Intelligence in Medicine
- `etichetta` 29 citazioni
- `etichetta` 2023

### Query-Guided Networks for Few-Shot Fine-Grained Classification and Person Search

*SOTTOTITOLO (h3)*

- `testo` Ritrovare una persona o distinguere categorie molto simili con pochissimi esempi.
- `etichetta` Pattern Recognition
- `etichetta` 39 citazioni
- `LINK` Elenco completo su Google Scholar →
- `LINK` Il codice su GitHub →

### Perché ogni articolo esce con il codice

*TITOLO DI SEZIONE (h2)*

- `testo` Un articolo che esce con il codice si può rifare da capo: chiunque scarica il repository, lancia gli esperimenti e ottiene gli stessi numeri. È il modo in cui ho lavorato per ogni pubblicazione, ed è lo stesso che porto nei progetti: quello che consegno si apre, si legge e si esegue.
- `testo` Alessandro Flaborea
- `testo` Consulenza e formazione sull'intelligenza artificiale. Dalla ricerca al software che le persone usano.
- `LINK` Prenota mezz'ora
- `testo` Cosa faccio
- `LINK` Servizi
- `LINK` Formazione
- `LINK` Che fine fanno i vostri dati
- `testo` Le prove
- `LINK` Lavori
- `LINK` Ricerca
- `LINK` Chi sono
- `LINK` Note
- `testo` Altrove
- `LINK` LinkedIn
- `LINK` GitHub
- `LINK` Google Scholar
- `LINK` English site
- `etichetta` © 2026 Alessandro Flaborea · P. IVA [DA INSERIRE]
- `LINK` Privacy
- `LINK` Cookie

---

## /chi-sono

Sorgente: `tavole/ChiSono.dc.html`

- `etichetta` Alessandro Flaborea
- `LINK` Servizi
- `LINK` Formazione
- `LINK` Lavori
- `LINK` Ricerca
- `LINK` Chi sono
- `LINK` Note
- `LINK` Parliamone
- `testo` [ LA TUA FOTO ]
- `testo` Sguardo in camera, luce naturale, sfondo neutro. Niente giacca se non la porti mai: chi ti incontra poi deve riconoscerti.
- `testo` In breve
- `testo` Dottorato in computer vision, Sapienza
- `testo` Dieci articoli pubblicati, il codice di tutti quelli che si potevano aprire
- `testo` Due anni da CTO di una startup industriale
- `testo` Quattro anni di aula all'università
- `testo` Chi sono

### Alessandro Flaborea

*TITOLO DI PAGINA (h1)*

- `testo` Ho cominciato a Udine, con una laurea in informatica e sei mesi in Svezia che mi hanno tolto l'idea di restare fermo. Poi Roma: una magistrale in data science e un dottorato al Perception and Intelligence Lab della Sapienza, dove ho passato tre anni su un problema che si può riassumere così — insegnare a un computer a guardare qualcuno che lavora e capire cosa sta facendo.
- `testo` Nel mezzo un inverno ad Amsterdam a lavorare su una geometria diversa da quella a cui siamo abituati, e quattro anni in aula come assistente alla didattica, che è dove ho scoperto che spiegare una cosa difficile a chi non la conosce è un mestiere a parte.
- `testo` Quella ricerca poi è diventata un prodotto. Ho co-fondato Procedo e ne sono stato CTO per due anni: video girati in reparto che diventano procedure scritte, dentro aziende vere, con clienti che pagavano e si lamentavano quando qualcosa non funzionava. È lì che ho imparato la parte che all'università non si impara, cioè quanto di quello che scrivi in un articolo sopravvive al contatto con un capannone.
- `testo` Ho lasciato il ruolo operativo a metà 2026 e oggi lavoro per conto mio. Il motivo è semplice: mi piace la parte in cui si entra in un'azienda che non conosco, si guarda come lavora e si capisce dove si perde tempo. In una società che cresce quella parte diventa sempre più piccola.
- `testo` Continuo a scrivere codice tutti i giorni e a pubblicare quello che imparo. Se mi chiami, la persona che viene in azienda e la persona che poi costruisce sono la stessa.
- `LINK` Prenota la mezza giornata diagnostica
- `LINK` Scarica il curriculum
- `LINK` Quello che ho costruito
- `testo` Come lavoro, in concreto

### Abitudini che vale la pena sapere prima

*TITOLO DI SEZIONE (h2)*


### Comincio dalla parte difficile

*SOTTOTITOLO (h3)*

- `testo` Nei progetti affronto per prima la cosa che potrebbe non funzionare. Costa qualche settimana in più all'inizio e fa risparmiare mesi quando la risposta è no.

### Scrivo tutto

*SOTTOTITOLO (h3)*

- `testo` Ogni decisione tecnica finisce in un documento leggibile anche da chi non programma. Serve a te se un domani il lavoro passa a qualcun altro.

### Dico quando conviene fermarsi

*SOTTOTITOLO (h3)*

- `testo` Capita che la cosa giusta da fare sia più piccola di quella che era stata chiesta, o che non vada fatta. Preferisco perdere un progetto che consegnarne uno inutile.
- `testo` Fuori dal lavoro
- `testo` Il filo che tiene insieme tutto è la curiosità, per problemi nuovi, posti nuovi e persone nuove. Mi ha portato da Udine a Roma e da Göteborg ad Amsterdam, e continua a decidere cosa costruisco nei fine settimana.
- `testo` Alessandro Flaborea
- `testo` Consulenza e formazione sull'intelligenza artificiale. Dalla ricerca al software che le persone usano.
- `LINK` Prenota mezz'ora
- `testo` Cosa faccio
- `LINK` Servizi
- `LINK` Formazione
- `LINK` Che fine fanno i vostri dati
- `testo` Le prove
- `LINK` Lavori
- `LINK` Ricerca
- `LINK` Chi sono
- `LINK` Note
- `testo` Altrove
- `LINK` LinkedIn
- `LINK` GitHub
- `LINK` Google Scholar
- `LINK` English site
- `etichetta` © 2026 Alessandro Flaborea · P. IVA [DA INSERIRE]
- `LINK` Privacy
- `LINK` Cookie

---

## /i-vostri-dati

Sorgente: `tavole/Dati.dc.html`

- `etichetta` Alessandro Flaborea
- `LINK` Servizi
- `LINK` Formazione
- `LINK` Lavori
- `LINK` Ricerca
- `LINK` Chi sono
- `LINK` Note
- `LINK` Parliamone
- `testo` I vostri dati

### Che fine fanno i vostri dati

*TITOLO DI PAGINA (h1)*

- `testo` È la domanda che quasi nessuno fa ad alta voce e che quasi tutti si fanno. Questa pagina risponde per esteso, senza rassicurazioni generiche, così potete decidere prima di firmare qualcosa.
- `testo` La risposta corta
- `testo` I vostri file restano vostri e nessuno li usa per addestrare niente. Quello che costruisco vive su un'infrastruttura intestata a voi; i documenti che mi date per capire il problema restano sul mio computer e alla fine del lavoro li cancello.
- `testo` Sotto c'è la versione lunga, che è quella che conta: le cose che avete appena letto vanno verificate, e qui trovate come.

### Situazioni che vengono confuse spesso

*TITOLO DI SEZIONE (h2)*

- `testo` Il rischio cambia moltissimo a seconda di cosa si sta facendo. Tenerle separate è già metà della risposta.
- `etichetta` 1

### Quando qualcuno in ufficio usa ChatGPT per conto suo

*SOTTOTITOLO (h3)*

- `testo` È la situazione più diffusa e la meno controllata. Quello che succede dipende quasi interamente da quale versione si sta usando: gli abbonamenti personali e quelli aziendali trattano i dati in modo diverso, e in alcuni casi il contenuto delle conversazioni può essere usato per migliorare il servizio, a meno che non si intervenga nelle impostazioni.
- `testo` La cosa da fare non è vietare lo strumento, perché verrebbe usato lo stesso di nascosto. È guardare insieme quali versioni sono in uso, che cosa dicono le loro condizioni oggi, e scrivere due righe di regola interna che le persone possano davvero seguire.
- `etichetta` 2

### Quando costruisco qualcosa per voi

*SOTTOTITOLO (h3)*

- `testo` Qui il controllo è totale, perché le scelte le facciamo noi. I dati stanno in un'infrastruttura intestata alla vostra azienda, in Europa, con le credenziali in mano a voi. Se domani finisce il rapporto, io esco e il sistema resta acceso. **Separazione per cliente.**
- `testo` Ogni azienda ha i propri dati isolati dagli altri, e la separazione è imposta dal database, non dal programma che ci gira sopra. È la differenza fra una regola che si può aggirare per errore e una che non si può. **Il minimo indispensabile esce.**
- `testo` Se serve un modello esterno, gli si manda il pezzo di informazione che serve a rispondere, non l'archivio. Nel portale ricambi, per esempio, il cliente finale vede la geometria che gli serve per riconoscere il pezzo e non il modello completo della macchina. **Registro degli accessi.**
- `testo` Chi ha visto cosa e quando resta scritto. Serve il giorno in cui qualcuno lo chiede. **Cancellazione su richiesta.**
- `testo` Quando il lavoro finisce, i dati che avete dato a me si cancellano e ve lo confermo per iscritto.
- `etichetta` 3

### Quando mi date dei documenti per capire il problema

*SOTTOTITOLO (h3)*

- `testo` Per la diagnosi servono file veri, non esempi ripuliti: un'estrazione dal gestionale, un elenco di articoli, qualche documento di lavoro. A seconda dell'azienda possono essere disegni, listini, schede di produzione, contratti o semplicemente mail. Restano sul mio computer, non finiscono in nessun servizio esterno, e alla fine del lavoro li cancello. Se preferite, si può firmare un accordo di riservatezza prima ancora del primo incontro: non me la prendo, è una richiesta ragionevole.
- `testo` Quando bastano dati finti per capire se una cosa funziona, uso dati finti. Capita più spesso di quanto si creda.
- `testo` Onestà

### Quello che non posso garantirvi

*TITOLO DI SEZIONE (h2)*

- `testo` Non controllo cosa fanno i fornitori esterni con i loro sistemi. Posso sceglierli, configurarli e leggervi le condizioni, ma le loro regole le scrivono loro e cambiano nel tempo.
- `testo` Nessuna configurazione tiene se le credenziali girano nelle chat. La parte più fragile di qualsiasi sistema sono le abitudini delle persone, ed è anche il motivo per cui insisto sulla formazione.
- `testo` Non sono un avvocato. Su contratti e adempimenti formali il vostro consulente ha l'ultima parola, e fa bene ad averla.
- `testo` Perché mi credete

### Su questo ho lavorato davvero

*TITOLO DI SEZIONE (h2)*

- `testo` In Procedo ogni cliente industriale aveva i propri dati isolati dagli altri, ed è un requisito che si progetta all'inizio o non si ottiene più. Nel portale ricambi la protezione della proprietà intellettuale è una sezione del progetto, decisa prima di scrivere il codice: per un costruttore, consegnare a un fornitore i disegni delle proprie macchine è la cosa più delicata che ci sia. Vale allo stesso modo per i listini di un distributore, per le schede clienti di uno studio o per le ricette di chi produce.
- `testo` Le stesse regole valgono per le cose che costruisco per me: la sicurezza sta nel database e il programma viene trattato come se fosse inaffidabile, perché prima o poi lo sarà.
- `testo` Se state guardando questa pagina perché qualcuno vi ha parlato dell'AI Act: la parte che riguarda la formazione del personale è in vigore dal febbraio 2025, e la trovate spiegata senza allarmismi nella pagina sulla formazione.
- `LINK` Vai alla formazione →
- `LINK` I quattro modi di lavorare insieme →

### Fatemi la domanda che qui non trova risposta

*TITOLO DI SEZIONE (h2)*

- `testo` Se è una buona domanda finisce su questa pagina, così la prossima azienda la trova già scritta.
- `LINK` Scrivimi
- `testo` Alessandro Flaborea
- `testo` Consulenza e formazione sull'intelligenza artificiale. Dalla ricerca al software che le persone usano.
- `LINK` Prenota mezz'ora
- `testo` Cosa faccio
- `LINK` Servizi
- `LINK` Formazione
- `LINK` Che fine fanno i vostri dati
- `testo` Le prove
- `LINK` Lavori
- `LINK` Ricerca
- `LINK` Chi sono
- `LINK` Note
- `testo` Altrove
- `LINK` LinkedIn
- `LINK` GitHub
- `LINK` Google Scholar
- `LINK` English site
- `etichetta` © 2026 Alessandro Flaborea · P. IVA [DA INSERIRE]
- `LINK` Privacy
- `LINK` Cookie

---

## /note

Sorgente: `tavole/Note.dc.html`

- `etichetta` Alessandro Flaborea
- `LINK` Servizi
- `LINK` Formazione
- `LINK` Lavori
- `LINK` Ricerca
- `LINK` Chi sono
- `LINK` Note
- `LINK` Parliamone
- `testo` Note

### Quello che sto imparando

*TITOLO DI PAGINA (h1)*

- `testo` Scrivo di quello che funziona dentro le aziende e di quello che non ha funzionato. In italiano, corto, più o meno una volta a settimana.
- `LINK` Chi scrive →
- `etichetta` 25 ago 2026

### Cosa chiede davvero l'AI Act alle piccole imprese

*TITOLO DI SEZIONE (h2)*

- `testo` Dal 2 febbraio 2025 chi usa strumenti di intelligenza artificiale deve garantire una formazione minima al personale ed essere in grado di dimostrarlo. Buona parte di quello che si legge in giro è allarmismo di chi vende corsi: qui c'è il testo dell'articolo 4, cosa comporta per un'azienda di trenta persone e cosa si riesce a fare in mezza giornata.
- `etichetta` Regole
- `etichetta` Da leggere in 5 minuti
- `etichetta` 18 ago 2026

### Perché ho costruito per primo il pezzo difficile

*TITOLO DI SEZIONE (h2)*

- `testo` In un progetto per un costruttore di macchine la parte più incerta era la conversione dei modelli CAD. L'ho affrontata prima del resto, con ottanta test. Se non avesse funzionato lo avremmo saputo dopo tre settimane, quando cambiare strada era ancora semplice.
- `etichetta` Come lavoro
- `etichetta` Manifattura
- `etichetta` 11 ago 2026

### Cosa succede ai tuoi dati quando incolli un preventivo in ChatGPT

*TITOLO DI SEZIONE (h2)*

- `testo` La risposta dipende da quale versione si sta usando e da come è configurata, e in azienda quasi nessuno lo sa. Tre cose che si possono controllare in dieci minuti.
- `etichetta` Rischi
- `etichetta` Da leggere in 5 minuti
- `etichetta` 4 ago 2026

### Metà delle richieste che ricevo si risolvono senza intelligenza artificiale

*TITOLO DI SEZIONE (h2)*

- `testo` Spesso il problema è che un'informazione che l'azienda già possiede non si riesce a recuperare quando serve. Sistemare quello costa meno e andrebbe comunque fatto prima.
- `etichetta` Opinioni
- `etichetta` 28 lug 2026

### Sei curriculum da due file di testo

*TITOLO DI SEZIONE (h2)*

- `testo` Quando i fatti sono scritti una volta sola, le varianti diventano sottrazioni da quel testo e smettono di essere copie da tenere allineate a mano. Un esempio piccolo di un principio che vale anche per le distinte base.
- `etichetta` Officina
- `etichetta` 21 lug 2026

### Tre cose che mi vengono chieste spesso e che di solito sconsiglio

*TITOLO DI SEZIONE (h2)*

- `testo` Il chatbot sul sito, il modello «addestrato sui nostri dati» e la dashboard che poi nessuno apre. Perché vengono chiesti, dove si inceppano e cosa si può fare al posto loro.
- `etichetta` Opinioni

### Te le mando per mail

*TITOLO DI SEZIONE (h2)*

- `testo` Una mail quando esce qualcosa di nuovo, niente altro. Ci si cancella con un clic.
- `testo` la.tua@mail.it
- `LINK` Iscrivimi
- `LINK` Oppure via RSS
- `testo` Alessandro Flaborea
- `testo` Consulenza e formazione sull'intelligenza artificiale. Dalla ricerca al software che le persone usano.
- `LINK` Prenota mezz'ora
- `testo` Cosa faccio
- `LINK` Servizi
- `LINK` Formazione
- `LINK` Che fine fanno i vostri dati
- `testo` Le prove
- `LINK` Lavori
- `LINK` Ricerca
- `LINK` Chi sono
- `LINK` Note
- `testo` Altrove
- `LINK` LinkedIn
- `LINK` GitHub
- `LINK` Google Scholar
- `LINK` English site
- `etichetta` © 2026 Alessandro Flaborea · P. IVA [DA INSERIRE]
- `LINK` Privacy
- `LINK` Cookie

---

## / su telefono

Sorgente: `tavole/HomeMobile.dc.html`

- `etichetta` Alessandro Flaborea
- `testo` Consulenza e formazione
- `testo` sull'intelligenza artificiale

### Aiuto le aziende a capire dove serve l'intelligenza artificiale e poi la costruisco.

*TITOLO DI PAGINA (h1)*

- `testo` Tre anni di dottorato in computer vision, poi due da CTO di una startup che quella ricerca l'ha portata dentro aziende che producono. Adesso lavoro in proprio.
- `LINK` Prenota mezz'ora
- `LINK` Guarda cosa ho costruito
- `testo` Dottorato in computer vision alla Sapienza · CVPR, ICCV, IROS · Co-fondatore e CTO di Procedo dal 2024 al 2026
- `testo` Il problema

### Le tre cose che mi vengono raccontate più spesso

*TITOLO DI SEZIONE (h2)*

- `testo` «Mi serve questo pezzo.»
- `testo` Arriva una mail con una foto storta. Prima di poter fare un preventivo bisogna capire di quale componente si tratti, e succede che ci si mettano in due per un paio di giorni.
- `testo` «Lo sa fare solo Bruno.»
- `testo` Quarant'anni di mestiere che stanno nella testa di una persona sola. Nessuno ha mai avuto il tempo di metterli per iscritto, e fra due anni Bruno va in pensione.
- `testo` «Abbiamo provato ChatGPT.»
- `testo` Qualcuno in ufficio ha cominciato a usarlo per conto suo. Non è chiaro con quali documenti, né se l'azienda sia d'accordo.
- `testo` Le prove

### Alcune cose che ho costruito

*TITOLO DI SEZIONE (h2)*

- `testo` Meccanica · commessa reale

### Portale ricambi 3D

*SOTTOTITOLO (h3)*

- `testo` Il cliente apre il modello 3D della macchina che ha comprato, clicca il pezzo che gli serve, e in azienda la richiesta arriva già associata al codice interno.
- `LINK` Come funziona →
- `LINK` Tutti i lavori, compresa la ricerca →

---

## mappa delle rotte — tavola di lavoro, non una pagina

Sorgente: `tavole/Rotte.dc.html`

- `testo` Architettura

### La mappa delle rotte

*TITOLO DI PAGINA (h1)*

- `testo` L'italiano diventa canonico. Niente viene distrutto: le pagine di oggi restano, cambiano indirizzo e cambia chi entra per primo.
- `etichetta` Italiano — canonico
- `etichetta` /
- `etichetta` La pagina del compratore
- `etichetta` /servizi
- `etichetta` Nuova · i quattro ingaggi
- `etichetta` /formazione
- `etichetta` Nuova · aule ed enti
- `etichetta` /i-vostri-dati
- `etichetta` Nuova · riservatezza, dal piè di pagina
- `etichetta` /note
- `etichetta` Nuova · scrittura, RSS, email
- `etichetta` /lavori
- `etichetta` Era
- `etichetta` /#work
- `etichetta` · ora pagina vera
- `etichetta` /lavori/[slug]
- `etichetta` I case study · invariati, +2 nuovi
- `etichetta` /ricerca
- `etichetta` Era
- `etichetta` /#research
- `etichetta` · contenuto intatto
- `etichetta` /chi-sono
- `etichetta` Era
- `etichetta` /#about
- `etichetta` · riscritta
- `etichetta` Inglese — il sito di oggi
- `etichetta` /en/
- `etichetta` Portfolio, così com'è
- `etichetta` /en/work/[slug]
- `etichetta` Case study in inglese
- `etichetta` Da sistemare subito **Il dominio.**
- `etichetta` astro.config.mjs
- `testo` dichiara
- `etichetta` flaborea.com
- `testo` , che non esiste: canonical, sitemap, OG e JSON-LD puntano tutti nel vuoto. Finché non è comprato,
- `etichetta` site
- `testo` torna a
- `etichetta` aleflabo.github.io
- `testo` e il CNAME si toglie.
- `etichetta` Reindirizzamenti
- `etichetta` /it/
- `etichetta` /
- `etichetta` permanente
- `etichetta` /#work
- `etichetta` /lavori
- `etichetta` alessandroflaborea.me
- `etichetta` flaborea.com
- `etichetta` quando è comprato
- `testo` I due case study da aggiungere
- `testo` Portale ricambi 3D
- `testo` Va in prima posizione sulla home italiana: è quello in cui un'azienda che produce si riconosce più in fretta.
- `testo` Tire Hub
- `testo` Sta fra i gestionali per materia, e la sua scheda dichiara che nasce da un'iniziativa e non da una commessa.
